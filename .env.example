# Get a key from TypeSafe and keep the real value in a local .env file.
TYPESAFE_API_KEY=replace_me

# Optional comma-separated browser origins allowed to call the local API.
# file:// previews use the built-in "null" origin allowance.
JEVTREE_ALLOWED_ORIGINS=
